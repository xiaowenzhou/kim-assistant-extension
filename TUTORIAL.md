# 🚀 Kim Assistant - 完整使用教程

> 浏览器插件 + 域名 + OpenClaw 集成，随时随地和 Kim AI 对话

---

## 📋 目录

1. [功能概览](#功能概览)
2. [架构图](#架构图)
3. [安装步骤](#安装步骤)
4. [配置插件](#配置插件)
5. [使用方法](#使用方法)
6. [安全设置](#安全设置)
7. [故障排除](#故障排除)

---

## 🎯 功能概览

| 功能 | 描述 |
|------|------|
| 💬 **随时对话** | 浏览器右下角浮动按钮，点击即可对话 |
| 🔗 **域名访问** | 使用 `https://your-domain.example.com` 无需端口 |
| 🔐 **安全认证** | HTTPS + API Key 双重保护 |
| 🖱️ **右键菜单** | 选中文字快速提问 |
| 📝 **快捷指令** | Summarize / Explain / Key Points |

---

## 🔧 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │           Kim Assistant 浏览器插件                     │  │
│  │   ✨ 浮动按钮 → 💬 聊天面板 → 📋 右键菜单            │  │
│  └─────────────────────────────────────────────────────┘  │
│                          │                                │
│                          ▼ HTTPS POST                      │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                   Nginx 反向代理                      │  │
│  │        https://your-domain.example.com/api/message          │  │
│  │                          │                            │  │
│  │         ┌────────────────┴────────────────┐         │  │
│  │         ▼                                      ▼         │  │
│  │   localhost:11434                        localhost:18789  │
│  │   测试服务器                                OpenClaw       │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    Telegram / Feishu / 其他通道
                              │
                              ▼
                          Kim AI
```

---

## 📦 安装步骤

### 步骤 1: 安装浏览器插件

#### Chrome / Edge

```bash
# 1. 进入插件目录
cd /root/.openclaw/workspace/kim-assistant-extension

# 2. 打开 Chrome 扩展管理
#    Chrome: chrome://extensions/
#    Edge:   edge://extensions/

# 3. 开启右上角「开发者模式」

# 4. 点击「加载已解压的扩展程序」

# 5. 选择 kim-assistant-extension 文件夹

# 6. ✅ 插件安装成功！
```

#### 效果预览

安装成功后：
- 工具栏出现 ✨ 图标
- 右键菜单出现「✨ Ask Kim」选项
- 右下角浮动按钮（可隐藏）

---

### 步骤 2: 配置插件

#### 方法 1: 通过插件图标

1. 点击工具栏 ✨ 图标
2. 点击「⚙️ Open Full Settings」
3. 填写配置（见下方）

#### 方法 2: 直接访问

在地址栏输入：`chrome-extension://[extension-id]/src/options/index.html`

#### 配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| **OpenClaw URL** | `https://your-domain.example.com` | 主 URL |
| **API Path** | `/api/message` | API 端点 |
| **API Key** | `kim-secret-key-2024` | 安全密钥 |

#### 完整配置截图

```
┌─────────────────────────────────────────┐
│  🔗 OpenClaw Connection                │
│                                         │
│  OpenClaw Gateway URL                   │
│  https://your-domain.example.com              │
│                                         │
│  API Path                              │
│  /api/message                          │
│                                         │
│  🔐 API Key (Recommended)              │
│  •••••••••••••••••••••                 │
│  kim-secret-key-2024                   │
│                                         │
│  [Test Connection]  [💾 Save]          │
└─────────────────────────────────────────┘
```

---

## 💡 使用方法

### 1. 打开聊天面板

**方法 A**: 点击右下角 ✨ 浮动按钮

**方法 B**: 点击工具栏图标 → 「Open Chat Panel」

**方法 C**: 快捷键（可选配置）

### 2. 发送消息

```
你: 今天天气怎么样？

Kim: 🌤️ 今天是晴天！气温 22-28°C，适合外出。
```

### 3. 右键菜单操作

在任意网页：

| 操作 | 方法 |
|------|------|
| 关于本页 | 右键 → ✨ Ask Kim → About this page |
| 总结选中文字 | 选中文字 → 右键 → ✨ Ask Kim → Summarize selection |
| 解释选中文字 | 选中文字 → 右键 → ✨ Ask Kim → Explain selection |

### 4. 快捷指令

在聊天面板输入框下方：

- 📝 **Summarize** - 总结当前页面
- 💡 **Explain** - 解释选中内容
- 🔑 **Key Points** - 提取关键点

### 5. 完整对话示例

```
用户: 帮我总结这个页面
Kim: # 页面总结
## 主要内容
- 介绍了 AI 助手的重要性
- 提供了多种使用场景
- 包含详细的技术架构

用户: 解释一下"反向代理"
Kim: # 反向代理
## 简单来说
反向代理就像一个前台接待：
1. 接收外部请求
2. 转发到内部服务器
3. 返回响应给请求方

## 优点
- 隐藏真实服务器
- 负载均衡
- SSL 加密
```

---

## 🔒 安全设置

### 1. API Key 管理

#### 默认 Key
```
kim-secret-key-2024
```

#### 自定义 Key（推荐）

```bash
# 生成强随机 Key
export API_KEY=$(openssl rand -base64 32)
echo $API_KEY

# 示例输出：
# abc123xyz789def456ghi789jkl012mno345pqr678stu901vwx234
```

### 2. Nginx 配置

```nginx
# /etc/nginx/sites-available/your-domain.example.com

server {
    listen 443 ssl http2;
    server_name your-domain.example.com;

    # SSL 证书
    ssl_certificate /root/.ssl/your-domain.example.com.crt;
    ssl_certificate_key /root/.ssl/your-domain.example.com.key;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    
    # CORS
    add_header Access-Control-Allow-Origin "*" always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type, X-API-Key" always;

    # API 代理
    location /api/message {
        proxy_pass http://127.0.0.1:11434;
        # ...
    }
}
```

### 3. 速率限制

```nginx
# Nginx 速率限制（可选）
limit_req_zone $binary_remote_addr zone=kim_api:10m rate=10r/s;
```

---

## 🛠️ 故障排除

### 问题 1: 插件无法连接

**症状**: 点击按钮没反应，连接超时

**解决方案**:
```bash
# 1. 检查测试服务器
curl https://your-domain.example.com/health

# 2. 检查 API
curl -sk https://your-domain.example.com/api/status \
  -H "X-API-Key: kim-secret-key-2024"

# 3. 重启测试服务器
pkill -f "secure-server.js"
cd /root/.openclaw/workspace/kim-assistant-extension/test-server
node secure-server.js
```

### 问题 2: API Key 无效

**症状**: `{"error":"Invalid API key"}`

**解决方案**:
1. 打开插件设置
2. 确认 API Key 正确
3. 重新保存设置

### 问题 3: SSL 证书警告

**症状**: 浏览器显示「不安全」

**解决方案**:
```bash
# 使用自签名证书（开发环境）
# 浏览器会显示警告，点击「高级」→「继续访问」

# 生产环境：使用 Let's Encrypt 免费证书
certbot --nginx -d your-domain.example.com
```

### 问题 4: 消息发送失败

**症状**: 发送后显示错误

**排查步骤**:
```bash
# 1. 测试直接访问
curl -sk https://your-domain.example.com/api/message \
  -H "X-API-Key: kim-secret-key-2024" \
  -d '{"text":"test"}'

# 2. 检查服务器日志
tail -f /var/log/nginx/access.log

# 3. 检查测试服务器日志
ps aux | grep secure-server
```

---

## 📁 文件结构

```
kim-assistant-extension/
├── manifest.json              # Chrome 扩展配置
├── README.md                  # 本文档
├── src/
│   ├── content/
│   │   ├── index.js          # 浮动聊天面板
│   │   └── styles.css        # UI 样式
│   ├── background/
│   │   └── index.js          # API 通信
│   ├── popup/
│   │   ├── index.html        # 快捷操作
│   │   └── popup.js          # 弹窗逻辑
│   └── options/
│       ├── index.html        # 设置页面
│       └── options.js        # 设置逻辑
└── test-server/
    ├── server.js             # 测试服务器
    ├── secure-server.js      # 安全版服务器
    └── TEST_GUIDE.md        # 测试指南
```

---

## 🔄 后续扩展

### 1. 连接真正的 Kim AI

```
浏览器插件
    │
    ▼ POST /api/message
Nginx 代理
    │
    ▼ POST /api/openclaw
OpenClaw Gateway (18789)
    │
    ▼
n8n 工作流
    │
    ├─► Telegram
    ├─► Feishu
    └─► Kim AI 回复
```

### 2. 部署到生产

```bash
# 1. 配置 SSL 证书（Let's Encrypt）
certbot --nginx -d your-domain.example.com

# 2. 设置开机自启
# 使用 systemd 或 pm2

# 3. 配置防火墙
ufw allow 443
```

---

## ✅ 快速检查清单

- [ ] 浏览器插件已安装
- [ ] 配置已保存（URL + API Key）
- [ ] 测试连接成功
- [ ] 可以发送消息
- [ ] 可以收到回复

---

## 📞 获取帮助

- **测试 API**: `https://your-domain.example.com/health`
- **查看状态**: `https://your-domain.example.com/api/status`
- **问题反馈**: 在 Telegram 联系

---

**最后更新**: 2026-02-08
**版本**: 1.0.0
